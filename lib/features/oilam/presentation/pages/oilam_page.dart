import 'package:togarak/core/exports.dart';

class OilamPage extends StatelessWidget {
  const OilamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: AppText(title: " Welcome Oilam"),
      ),
    );
  }
}
